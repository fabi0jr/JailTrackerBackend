services:
  postgres-db:
    image: postgres:15
    container_name: jailtracker-postgres
    restart: always
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: jailtracker
    ports:
      - "5432:5432"